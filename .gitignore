# ── secrets (never commit) ────────────────────────────────
AI/machine_config.json

# ── superseded/archived material ─────────────────────────
_archive/
KeepItGreen_backend.zip
app_offline.htm

# ── .NET ──────────────────────────────────────────────────
Recycle/bin/
Recycle/obj/
Recycle/publish/
*.user

# ── Flutter / Android ────────────────────────────────────
Mobile/build/
Mobile/.dart_tool/
Mobile/.flutter-plugins*
Mobile/android/.gradle/
Mobile/android/local.properties
Mobile/android/app/.cxx/
*.apk

# ── Python ────────────────────────────────────────────────
__pycache__/
*.pyc
AI/captures/
AI/pending_transactions.jsonl
AI/runs/

# ── Node (doc/deck generators) ────────────────────────────
node_modules/
package-lock.json

# ── OS / editor noise ─────────────────────────────────────
Thumbs.db
.DS_Store
*.tmp
.claude/
# Office lock/temp files (e.g. ~$deck.pptx)
~$*

# NOTE: Recycle/ (the backend) is ALSO the team's own repo
# (github.com/shahdyounis77/Recycle). Its files are included here as a full
# project snapshot; its nested .git is ignored so it isn't added as a submodule.
Recycle/.git/

# ── Android release signing (keep the keystore backed up OUTSIDE git) ──
Mobile/android/key.properties
Mobile/android/app/*.keystore

# ── DB backup: contains real user data — never publish ──
Database/*.bak
